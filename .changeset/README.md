# Changesets

각 변경마다 `pnpm changeset`을 실행해 어떤 패키지가 어떤 단계(major/minor/patch)로
bump되는지 기록합니다. `pnpm version-packages`가 이 기록을 모아 버전을 올리고
CHANGELOG를 갱신합니다.

See https://github.com/changesets/changesets for full docs.
