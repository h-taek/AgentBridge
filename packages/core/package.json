{
  "name": "@agentbridge/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared core logic for AgentBridge desktop app and VS Code extension",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "typescript": "^5.6.3"
  }
}
